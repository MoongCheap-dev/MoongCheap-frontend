/**
 * 스플래쉬(B-01 세션 확인) 문구. 시안 `1318:13032`(기본) · `1372:6186`(로딩 지연) 그대로 옮겼다.
 *
 * 화면 ID가 따로 없다. 기능명세서 FN-B01-03이 스플래쉬를 독립 화면이 아니라 로그인(B-01)의
 * 구성 요소로 정의한다("스플래시 화면(로고), 별도 조작 UI 없음"). 그래서 `screens.ts`에도 없다.
 */
export const SPLASH_MESSAGES = {
  /** 워드마크 아래 한 줄. 두 시안 공통. */
  tagline: '뭉치면 싸진다!',
  /**
   * 로딩이 길어졌을 때만 나오는 안내. 시안 「후보2」에만 있다.
   *
   * ⚠️ 시안은 `찾는중`으로 붙여 썼는데 여기서는 `찾는 중`으로 띄웠다. 의존명사 `중`은 앞말과
   *    띄어 쓰는 게 맞춤법이라 시안 쪽 오타로 본다. 시안과 어긋나는 유일한 문구다.
   */
  loading: '뭉치가 최저가를 찾는 중',
  /** 워드마크 대체 텍스트. 시안에 없는 값이라 화면에는 보이지 않는다. */
  wordmarkAlt: '뭉치',
} as const;
